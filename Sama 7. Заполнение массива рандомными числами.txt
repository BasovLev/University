#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{
    int i, left, right, a, c=40, d=0, me[c], n, *p, sum;
    double m;
    n=0;
    m=0.0;
    /*scanf("\n", &left); Надо спросить! Что делать с rand? В видео и методичке ничего нет.
    scanf("\n", &right);
    *p = me;
    for(i=0; i<c; i++){
        d=(int) rand() / (RAND_MAX + 1.1)
        
        if (d=0){
            *(p+i)= (int) rand() / (left + RAND_MAX)
        }
        if (d=1){
            *(p+i)= (int) rand() / (RAND_MAX + right)
        }
        
        printf("%d %d\n", i, *(p+i));
    }*/
    /* Индексы
    for(i=0; i<c; i++)
    {
        scanf("%d",&me[i]);
    }
    scanf("%d", &a);
    
    for(i=0; i<c; i++)
    {
        if(me[i]>a)
        {
           m+=me[i];
           n++;
       }
    }
    if(n==0){
        m=0;
    }
    else{
        m=m/n;
    }
    printf("%.4f\n",m);*/
      // Указатели
    p = me;
    for(i=0; i<c; i++){
        
        scanf("%d", (p+i));
        
    }

    scanf("%d", &a);
    for(i=0; i<c; i++){
        if(*(p+i)>a){
            
           m+=*(p+i);
           n++;
       }
    }
    if(n==0){
        
        m=0; 
    }
    else
    {
        
        m=m/n;
    }
    p=me;
    sum=0;
    for(i=0; i<c; i++){
        if(m > *(p+i)){
            
           sum += *(p+i);
       }
    }
    printf("%.4f\n", m);
    printf("%d\n", sum);
    return 0;
}